// ZIM Smart Queue — Brand Color System
// Source: Option 2 (White background + navy letters)

export const colors = {
  // ── Gold Palette ──────────────────────────────────────────
  primary: '#D4AF37',               // Metallic gold — main accent
  primaryDark: '#A07D20',           // Deep gold
  primaryLight: '#F0C952',          // Bright gold
  primaryMuted: 'rgba(212, 175, 55, 0.15)', // Gold wash

  // ── Navy Palette (Now used for text/accents instead of bg)
  background: '#FAFAFA',            // Soft white — main background
  backgroundAlt: '#F5F7FA',         // Light gray/blue — elevated bg
  secondary: '#0F1C33',             // Deep navy — secondary

  // ── Text ──────────────────────────────────────────────────
  textPrimary: '#0F1C33',           // Navy — main text
  textSecondary: '#4A5568',         // Slate gray — secondary text

  // ── Surfaces & Borders ────────────────────────────────────
  surface: '#FFFFFF',               // White surface
  surfaceBorder: '#E2E8F0',         // Light gray border
  inputBackground: '#F8FAFC',       // Light input bg

  // ── Semantic ──────────────────────────────────────────────
  error: '#FF6B6B',
  success: '#4CAF82',
};
